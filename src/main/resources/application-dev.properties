spring.application.name=crime-report

# 1. PostgreSQL driver
spring.datasource.driver-class-name=org.postgresql.Driver

# 2. Supabase JDBC URL
# Copy it from Supabase dashboard → Connect → Type: JDBC (Session Pooler recommended)
spring.datasource.url=jdbc:postgresql://db.npeqrfkymstswqnjpzxm.supabase.co:5432/postgres

# 3. Credentials (often embedded in URL, but also safe to include separately)
spring.datasource.username=postgres
spring.datasource.password=crimereport1@#$%

# 4. Optional JPA settings
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=create-drop
